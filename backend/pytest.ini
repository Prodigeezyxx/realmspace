[pytest]
testpaths = tests
# put backend/ on sys.path so `import app` works without installing the package
pythonpath = .
asyncio_mode = auto
# every async fixture lives as long as the test that asked for it unless it
# says otherwise
asyncio_default_fixture_loop_scope = function
