/**
 * realmspace canonical contracts.
 *
 * Import from `@/lib/contracts` everywhere. Both the mocked prototype and the
 * real backend conform to these types. See:
 *   - docs/event-bus-spec.md   (events)
 *   - docs/data-model.md       (graph)
 *   - docs/consent-and-identity.md (consent/PII gating)
 */
export * from "./events";
export * from "./graph";
